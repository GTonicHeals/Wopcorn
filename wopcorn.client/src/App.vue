<script setup lang="ts">
import { RouterView } from 'vue-router';

// The shell lives on the authenticated routes (AppShell). App.vue is only the
// mount point, so the auth screens render without any chrome.
</script>

<template>
  <RouterView />
</template>
