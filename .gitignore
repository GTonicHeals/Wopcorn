# Build output
bin/
obj/
dist/

# Tooling / IDE
.vs/

# Client dependencies
node_modules/

# Local SQLite database (wopcorn.db, -wal, -shm)
wopcorn.db*

# Host settings: TMDB token and SMTP password in plain text
deploy/wopcorn.host.json

# curl cookie jars: these hold a live wopcorn.auth session cookie
c.txt
cookies.txt
*.cookies
